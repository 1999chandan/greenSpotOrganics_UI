# Green Spot Organics - Environment Configuration

# API Configuration
# Backend API base URL - adjust based on environment
VITE_API_BASE_URL=http://localhost:3000/api

# Environment
VITE_ENV=development

# Feature Flags
VITE_ENABLE_ADMIN=true
VITE_ENABLE_NOTIFICATIONS=true
VITE_ENABLE_ANALYTICS=false

# Authentication
# Token refresh interval in milliseconds (default: 5 minutes)
VITE_TOKEN_REFRESH_INTERVAL=300000

# Payment Gateway (if applicable)
VITE_PAYMENT_KEY=your_payment_key_here
VITE_PAYMENT_SECRET=your_payment_secret_here

# Analytics (if applicable)
VITE_ANALYTICS_ID=your_analytics_id_here

# AWS S3 or Image CDN (if applicable)
VITE_IMAGE_CDN_URL=https://cdn.example.com
VITE_AWS_S3_BUCKET=your-bucket-name
VITE_AWS_REGION=us-east-1

# Sentry/Error Tracking (if applicable)
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Notes:
# - VITE_API_BASE_URL: Base URL for all API calls. Include /api suffix if needed.
#   Development: http://localhost:3000/api
#   Production: https://api.greenspotorganics.com/api
#
# - Token refresh happens automatically via axios interceptors
# - All VITE_ prefixed variables are exposed to client-side code
# - Sensitive keys (secrets) should NEVER be prefixed with VITE_
#
# Development Setup:
# 1. Copy this file to .env.local
# 2. Update VITE_API_BASE_URL to match your backend server
# 3. Restart dev server: npm run dev
#
# Production Setup:
# 1. Set environment variables on deployment platform
# 2. Ensure sensitive keys are NOT exposed to client
# 3. Use VITE_API_BASE_URL pointing to production backend
